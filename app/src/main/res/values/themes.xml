<resources xmlns:tools="http://schemas.android.com/tools">
    <style name="Theme.Fairly" parent="Theme.MaterialComponents.DayNight.NoActionBar" />

    <style name="Theme.Fairly.Starting" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/white</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Fairly</item>
    </style>
</resources>